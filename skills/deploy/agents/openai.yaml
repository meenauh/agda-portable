interface:
  display_name: "AGDA: Deploy"
  short_description: "Version, tag, merge, and push releases"
  default_prompt: "Use $AGDA:deploy to bump the project version, merge the release or hotfix, tag it, push branches, and return to develop."
  brand_color: "#2563EB"
