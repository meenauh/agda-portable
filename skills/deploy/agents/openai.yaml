interface:
  display_name: "AGDA: Deploy"
  short_description: "Deploy a Railway release or hotfix"
  default_prompt: "Use $AGDA:deploy to deploy a Railway release or hotfix, verify the Railway result, run the production smoke check, and return to develop."
  brand_color: "#2563EB"
